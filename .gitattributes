*.otbm filter=lfs diff=lfs merge=lfs -text
*.lib filter=lfs diff=lfs merge=lfs -text
build-msvc/** filter=lfs diff=lfs merge=lfs -text

# Auto detect text files and perform LF normalization
* text=auto
# Custom for Visual Studio
*.sln     merge=union
*.vcxproj merge=union
